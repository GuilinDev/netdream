# Core scientific stack
numpy>=1.24
scipy>=1.11
matplotlib>=3.7

# PyTorch + PyG
torch>=2.0
torch-geometric>=2.4

# RL + environments
gymnasium>=0.29
stable-baselines3>=2.0
grid2op>=1.10

# Cluster / control
pyyaml>=6.0
tqdm>=4.66

# Optional: load testing for live cluster runs
locust>=2.20
